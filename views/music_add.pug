extends layout

block content
	h1 #{ title }
	form(method="POST", action="/music/add")
		.form-group
			label Ismi:
			input.form-control(name="name", type="text")
		.form-group
			label Izoh:
			textarea.form-control(name="comment")
		br
		input.btn.btn-success(type="submit", value="yubor")
